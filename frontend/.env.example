# Copy to .env to change the server address. The default is http://localhost:8000.
# VITE_API_BASE=http://localhost:8000

# Use the built-in fixtures (src/mock) instead of a running server:
# VITE_API_BASE=mock
